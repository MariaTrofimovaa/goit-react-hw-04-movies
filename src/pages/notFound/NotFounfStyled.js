import styled from "styled-components";

export const NotFoundContainer = styled.div`
  .container {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translateX(-50%);
  }
  .title,
  .text {
    text-align: center;
  }
  .title {
    color: rgba(77, 60, 60, 0.384);
    font-size: 40px;
  }
  .text {
    color: rgba(77, 60, 60, 0.384);
    font-size: 25px;
  }
`;
